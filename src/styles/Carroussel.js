import { styled } from "styled-components";

export const CarrousselStyles = styled.div`
  .carousel-control-next-icon,
  .carousel-control-prev-icon {
    background-color: none;
    width: 4em;
  }

  img {
    filter: sepia(10px) grayscale(20%);
  }
`;
